# Copy to backend.env (optional) if your tooling loads dotenv for local demos.
# FastAPI host example listens here by default.
TASKBRIDGE_EXAMPLE_BASE_URL=http://127.0.0.1:8000

# Android emulator reaches host loopback via:
# TASKBRIDGE_EXAMPLE_BASE_URL=http://10.0.2.2:8000
